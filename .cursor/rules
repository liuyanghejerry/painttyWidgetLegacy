# PainttyWidget Qt/C++ 项目规则

## 代码风格
- 使用 4 空格缩进
- 使用驼峰命名法 (camelCase) 用于变量和函数
- 使用帕斯卡命名法 (PascalCase) 用于类名
- 使用下划线命名法 (snake_case) 用于文件名
- 大括号使用 K&R 风格 (左大括号不换行)
- 每行最大长度：120 字符

## 文件命名规范
- 头文件：`*.h`
- 源文件：`*.cpp`
- UI 文件：`*.ui`
- 资源文件：`*.qrc`
- 翻译文件：`*.ts`, `*.qm`

## 目录结构
- `src/common/` - 通用组件和工具
- `src/painttyDesktop/` - 主应用程序
- `src/painttyDesktop/widgets/` - UI 组件
- `src/painttyDesktop/paintingTools/` - 绘画工具
- `src/painttyDesktop/misc/` - 杂项功能
- `src/painttyDesktop/fonts/` - 字体文件
- `src/painttyDesktop/iconset/` - 图标和样式

## Qt 特定规则
- 使用 Qt 6 语法和 API
- 优先使用 Qt 容器类 (QList, QMap, QHash 等)
- 使用 Qt 的信号槽机制进行对象间通信
- 使用 Qt 的智能指针管理内存
- 遵循 Qt 的命名约定

## 类设计原则
- 每个类应该有明确的职责
- 使用虚函数实现多态
- 提供适当的构造函数和析构函数
- 使用 const 成员函数标记不修改对象状态的函数
- 优先使用引用参数传递大对象

## 错误处理
- 使用 Qt 的异常处理机制
- 提供有意义的错误消息
- 使用 Q_ASSERT 进行调试断言
- 记录关键操作的日志

## 国际化
- 使用 tr() 函数包装所有用户可见的字符串
- 支持多语言翻译
- 使用 QLocale 处理本地化

## 性能优化
- 避免不必要的对象拷贝
- 使用 const 引用传递参数
- 合理使用 Qt 的缓存机制
- 优化绘图操作

## 文档规范
- 为公共 API 提供详细的文档注释
- 使用 Doxygen 风格的注释
- 说明函数的参数、返回值和异常

## 构建和部署
- 使用 build.sh 脚本构建
- 支持跨平台编译
- 正确处理依赖关系
- 提供适当的安装脚本 